<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pers.sjh.cloud_note.mapper.account.UserMapper">
   <select id="findByName" resultType="User" parameterType="String">
       select * from user where loginName = #{username}
   </select>
    <update id="update" parameterType="User">
        update user
        set
            loginName = #{loginName},
            password = #{password},
            email = #{email},
            realName = #{realName},
            mobileNo = #{mobileNo},
            departmentId = #{departmentId},
            enabled = #{enabled},
            lastAccess = #{lastAccess},
            deleted = #{deleted},
            updater = #{updater},
            updateTime = NOW()
        where id = #{id}
    </update>
    <update id="delete" parameterType="User">
        update user
        set
            deleted = #{deleted},
            updater = #{updater},
            updateTime = NOW()
        where id = #{id}
    </update>
</mapper>